package com.vungle.publisher;

/* compiled from: vungle */
public abstract class jm<T extends jn<T, P, E, ?, ?>, P extends jm<T, P, E>, E extends jo<P>> extends cp<T, P, E> {

    /* compiled from: vungle */
    public static abstract class a<T extends jn<T, P, E, A, V>, P extends jm<T, P, E>, E extends jo<P>, A extends jk<A, V, ?>, V extends jj<A>> extends com.vungle.publisher.cp.a<T, P, E> {
        protected a() {
        }
    }
}
